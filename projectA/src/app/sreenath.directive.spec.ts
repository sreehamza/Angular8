import { SreenathDirective } from './sreenath.directive';

describe('SreenathDirective', () => {
  it('should create an instance', () => {
    const directive = new SreenathDirective();
    expect(directive).toBeTruthy();
  });
});
