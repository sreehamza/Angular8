import { HightDirective } from './hight.directive';

describe('HightDirective', () => {
  it('should create an instance', () => {
    const directive = new HightDirective();
    expect(directive).toBeTruthy();
  });
});
